{
  "name": "fabricx-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run script/deploy.ts --network xlayer"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "hardhat": "^2.22.0",
    "typescript": "~5.6.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0"
  }
}
